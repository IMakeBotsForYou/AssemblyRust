a dd, 0
b dd, 1
c dd, 0

mov DX, 50

loop_: 
   mov CX, DX
loop:
   mov EAX, [a]
   add EAX, [b]
   mov [c], EAX

   mov EAX, DWORD PTR [b]
   mov DWORD PTR [a], EAX
   mov EAX, DWORD PTR [c]
   mov DWORD PTR [b], EAX
   print DWORD PTR [c]
   dec DX
   cmp DX, 0
   jle exit
   dec CX
   cmp
   jnz loop_
exit:
